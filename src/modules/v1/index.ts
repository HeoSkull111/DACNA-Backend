import userRouter from "@user/user";
import workdayRouter from "@workday/workday";
import groupRouter from "@group/group";

export const routers = {
  user: userRouter,
  workday: workdayRouter,
  group: groupRouter,
};
